# ステップ 14.19: 関数ポインタ typedef と配列 typedef

## 概要

関数ポインタの typedef（`typedef void (*fn_ptr)(void)`）と配列の typedef（`typedef int arr[10]`）のサポートを追加する。また、typedef 名で `sizeof` が動作するように修正する。

## なぜ必要か

PostgreSQL では関数ポインタの typedef が広く使われている:

```c
typedef void (*pg_funcptr_t)(void);
typedef int (*comparator)(const void *, const void *);
typedef void (*MemoryContextCallbackFunction)(void *arg);
```

配列の typedef も出現する:

```c
typedef int32 TransactionId;
typedef TransactionId MultiXactId;
```

## 実装

### 関数ポインタ typedef

typedef の解析において、戻り値の型を取得した後、`(*Name)` パターンをチェックする:

```rust
if self.current().kind == TokenKind::LParen
    && self.pos + 1 < self.tokens.len()
    && self.tokens[self.pos + 1].kind == TokenKind::Star
{
    // Parse: typedef RetType (*Name)(params...)
    self.advance(); // (
    self.advance(); // *
    // skip qualifiers
    let name = self.parse_ident();
    self.expect(TokenKind::RParen);
    // Skip parameter list
    self.skip_paren_group();
    self.expect(TokenKind::Semicolon);
    self.typedefs.insert(name, Type::ptr_to(return_type));
}
```

関数ポインタは `Ptr(return_type)` として格納される。これは既存の `FuncPtrCall` メカニズムを通じた関数ポインタ呼び出しで動作する簡略化された表現である。

### 配列 typedef

基本型と名前を取得した後、`[N]` をチェックする:

```rust
if self.current().kind == TokenKind::LBracket {
    self.advance();
    let size = self.eval_const_expr();
    self.expect(TokenKind::RBracket);
    let arr_ty = Type { kind: TypeKind::Array(Box::new(base), size), ... };
    self.typedefs.insert(name, arr_ty);
}
```

### sizeof の修正

`sizeof(type)` の検出で `is_type_keyword()` の代わりに `is_type_start()` を使用するように変更し、typedef 名での sizeof を動作可能にした:

```rust
// Before: Self::is_type_keyword(&self.tokens[self.pos + 1].kind)
// After:  self.is_type_start(&self.tokens[self.pos + 1].kind)
```

これにより `sizeof(my_typedef)` が正しく typedef を参照してサイズを返すようになる。

### トップレベルと文レベルの両方

トップレベルの `parse()` と文レベルの `stmt()` の両方の typedef ハンドラが、関数ポインタと配列のパターンに対応するようになった。

## テストケース

```c
typedef void (*fn_ptr)(void); int main() { return 5; }    // → 5
typedef int (*binop)(int, int);
int add(int a, int b) { return a + b; }
int main() { binop f = add; return f(1, 2); }             // → 3
typedef int arr10[10]; int main() { return sizeof(arr10); } // → 40
```
