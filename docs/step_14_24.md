# ステップ 14.24: 構造体メンバを介した関数ポインタ呼び出し

## 概要

構造体メンバに格納された関数ポインタの呼び出しをサポートする。直接メンバアクセス（`s.fp(args)`）とポインタ経由のメンバアクセス（`p->fp(args)`）の両方に対応する。また、構造体定義内での関数ポインタメンバの宣言もサポートする。

## この実装が重要な理由

PostgreSQLでは構造体内の関数ポインタメンバが多用されている:

```c
typedef struct FmgrInfo {
    PGFunction fn_addr;     // typedef for function pointer
    Oid fn_oid;
    short fn_nargs;
} FmgrInfo;

// Called as: info->fn_addr(fcinfo)

typedef struct MemoryContextMethods {
    void *(*alloc)(MemoryContext context, Size size);
    void (*free_p)(MemoryContext context, void *pointer);
    void (*reset)(MemoryContext context);
} MemoryContextMethods;

// Called as: context->methods->alloc(context, size)
```

## 実装

### 1. 構造体定義内の関数ポインタメンバ

構造体メンバのパース時に、`parse_type()` が戻り値の型を返した後、`(*name)(params)` パターンをチェックして関数ポインタメンバかどうかを判定する:

```rust
if self.current().kind == TokenKind::LParen
    && self.pos + 1 < self.tokens.len()
    && self.tokens[self.pos + 1].kind == TokenKind::Star
{
    // Parse: return_type (*member_name)(param_types...)
    self.advance(); // (
    self.advance(); // *
    let name = self.parse_ident();
    self.expect(TokenKind::RParen);
    // Skip parameter list
    self.skip_paren_group();
    mem_ty = Type::ptr_to(Type::void()); // simplified representation
}
```

関数ポインタは `Ptr(Void)` として格納される。x86-64では8バイトであり、あらゆる関数ポインタに対して正しいサイズである。

### 2. メンバアクセスを介した関数ポインタ呼び出し

`postfix()` パーサーで、`.` または `->` から `Member` 式を構築した後、次のトークンが `(` かどうかをチェックする:

```rust
TokenKind::Dot => {
    // ... parse member name, create Member node ...

    // Check for function pointer call: s.fp(args)
    if self.current().kind == TokenKind::LParen {
        self.advance();
        let args = self.parse_arg_list();
        self.expect(TokenKind::RParen);
        node = Expr::FuncPtrCall { fptr: Box::new(node), args };
    }
}
```

同じロジックが `->` （アロー）メンバアクセスにも適用される。

### 動作の仕組み

`s.fp(3, 4)` の場合:

1. パーサーがメンバアクセスとして `Member(Var("s"), "fp")` を作成
2. メンバ名の後に `(` を検出 → `FuncPtrCall` でラップ
3. 結果: `FuncPtrCall { fptr: Member(Var("s"), "fp"), args: [3, 4] }`

`FuncPtrCall` のコード生成:
1. `fptr` を評価 → 構造体メンバから関数ポインタの値をロード
2. ポインタを `%r10` に移動
3. 引数をレジスタにセットアップ
4. `*%r10` を呼び出し

## テストケース

```c
// Function pointer member via typedef
unsigned long myfn(void *a) { return 42; }
typedef unsigned long (*fn_t)(void *);
typedef struct { fn_t fn; } S;
int main() { S s; s.fn = myfn; return (int)s.fn(0); }              // → 42

// Direct function pointer member syntax
int add(int a, int b) { return a+b; }
typedef struct { int (*f)(int, int); } S;
int main() { S s; s.f = add; return s.f(3, 4); }                   // → 7

// Arrow notation through pointer
int get(void) { return 10; }
typedef struct { int (*f)(void); } S;
int main() { S s; s.f = get; S *p = &s; return p->f(); }           // → 10
```
